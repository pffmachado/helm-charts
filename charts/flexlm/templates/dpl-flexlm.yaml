apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -c -f flexlm.yml
    kompose.version: 1.31.2 (HEAD)
  creationTimestamp: null
  labels:
    io.kompose.service: flexlm
  name: flexlm
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: flexlm
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -c -f flexlm.yml
        kompose.version: 1.31.2 (HEAD)
      creationTimestamp: null
      labels:
        io.kompose.network/docker-default: "true"
        io.kompose.service: flexlm
    spec:
      containers:
        - name: flexlm
          image: ghcr.io/hbkdaq/flexlm
          command: ["/opt/flexlm/bin/lmgrd"]
          args: ["-z", "-c", "/opt/flexlm/licenses/0"]
          ports:
            - containerPort: 27000
              hostPort: 27000
              protocol: TCP
          resources: {}
      imagePullSecrets:
        - name: hbkdaq
      restartPolicy: Always
status: {}
