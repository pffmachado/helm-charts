apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: cdash
  name: cdash
spec:
  ingressClassName: traefik
  rules:
    - host: {{ .Values.applicationHost }}
      http:
        paths:
          - backend:
              service:
                name: cdash
                port:
                  number: 443
            path: /
            pathType: Prefix
status:
  loadBalancer: {}
