apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert -c -f cdash.yml
    kompose.version: 1.31.2 (HEAD)
  creationTimestamp: null
  labels:
    io.kompose.service: database
  name: database
spec:
  ports:
    - name: {{ .Values.externalDatabase.port | quote }}
      port: {{ .Values.externalDatabase.port }}
      targetPort: {{ .Values.externalDatabase.port }}
  selector:
    io.kompose.service: database
status:
  loadBalancer: {}
